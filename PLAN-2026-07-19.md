# TSAE Web — แผนแก้ไข 2026-07-19

## บทสรุป
สำรวจทั้งเว็บ (Astro static + Python FastAPI backend) พบปัญหา "แก้ไม่หาย" ที่ผู้ใช้รายงาน
ส่วนใหญ่มาจาก **ตารางในหน้า conference ใช้ `overflow-hidden` แทน `overflow-x-auto`**
ทำให้ข้อมูลคอลัมน์ที่ยาว (ชื่อสังกัดภาษาอังกฤษ) ถูกคลิป/กองในคอลัมน์แรกบนมือถือ และไม่มี scroll แนวนอน

## สถานะการแก้

### แก้แล้วในเซสชันนี้
| ไฟล์:บรรทัด | ปัญหา | การแก้ |
|---|---|---|
| `src/pages/2026conf-national/index.astro:373` | ตารางค่าลงทะเบียน `overflow-hidden` → คลิป ไม่ scroll | เปลี่ยนเป็น `overflow-x-auto` + `min-w-[480px]` + `whitespace-nowrap` บน td |
| `src/pages/2026conf-national/index.astro:522` | ตารางคณะกรรมการวิชาการ 19 แถว ชื่อสังกัดยาว | `overflow-x-auto` + `min-w-[560px]` + `w-[40%]` บนคอลัมน์รายชื่อ + `align-top` |
| `src/pages/2026conf-intl/index.astro:367` | ตาราง Registration Fees 2 ตัว (Online/Onsite) | แยก `overflow-hidden` (header) จาก `overflow-x-auto` (table) + `min-w-[440px]` |
| `src/pages/2026conf-intl/index.astro:623` | ตาราง Scientific Committee 19 แถว | เหมือน national: `overflow-x-auto` + `min-w-[560px]` + `w-[40%]` + `align-top` |

### แก้แล้วในเซสชันก่อน (ยืนยันยังอยู่)
- `src/components/InlineEditor.astro:142` — picker grid `repeat(auto-fit, minmax(140px,1fr))`
  แทน `repeat(2,1fr)` + `min-height: 56px` บนปุ่ม + modal padding ลดลง
- Admin CMS sidebar ภาษาไทยทั้งหมด (ผู้ใช้แก้ใน Cursor เอง)

### ตรวจสอบแล้ว ไม่มีปัญหา
- `src/components/pages/MembersPage.astro:68` — มี `overflow-x-auto` ถูกต้อง
- `src/components/pages/CoordinatorsPage.astro:58,84` — มี `overflow-x-auto` ถูกต้อง
- `data/cms/pages.json` — ไม่มี `<table>` ฝังใน richtext
- `server/registration/app.py` admin table CSS (บรรทัด ~750) — มี `.table-wrap{overflow-x:auto}` + `min-width:880px` + `table-layout:fixed` ถูกต้อง

## ปัญหาที่ยังไม่ได้แก้ (รอคำสั่ง)
| หัวข้อ | รายละเอียด | ความเสี่ยง |
|---|---|---|
| Spam submissions | 65/67 ถูกจับถูก 0 false positive — ระบบทำงานดี ไม่จำเป็นต้องแก้ด่วน | low |
| Spam ที่หลุดเข้า inbox หลัก | มี 2 รายการที่เป็นของแท้ ยังไม่เห็น pattern สแปมใหม่ | low |
| Auto-purge spam เก่า | endpoint `purgespam` มีอยู่ใน `app.py:1643` แต่ต้องกด manual | low |
| Poster grid `grid-cols-2` ไร้ breakpoint | `src/pages/2026conf-intl/index.astro:658` รูปอาจเล็กเกินบนจอแคบมาก | low |

## ตรวจสอบหลังแก้
1. รัน `./deploy.sh web` เพื่อเผยแพร่
2. บนมือถือ (viewport ≤ 375px) เปิด:
   - `/2026conf-national/#fees` — ตารางค่าลงทะเบียนต้อง scroll แนวนอนได้
   - `/2026conf-national/#committees` — ตารางกรรมการวิชาการต้อง scroll ได้ ชื่อสังกัดไม่กอง
   - `/2026conf-intl/#fees` — ตาราง Online/Onsite 2 ใบ แต่ละใบ scroll ได้
   - `/2026conf-intl/#committees` — Scientific Committee scroll ได้
3. บน desktop ตารางต้องไม่บานออกจาก container (`min-w` ใช้แค่ตอนจอแคบ)

## หมายเหตุสถาปัตยกรรม
- Astro เป็น static หน้า admin ที่ `/api/admin/*` มาจาก Python FastAPI (container `tsae_registration`)
- ดังนั้นแก้ frontend Astro แล้วต้อง `./deploy.sh web` ส่วน admin layout ต้อง `./deploy.sh api`
- ฐานข้อมูล submissions อยู่ที่ `/data/registrations.db` ใน container (SQLite)
